substitutions:
  project_name: "ajs.HealthCheck"
  project_version: "3.5.49"

http_request:
  useragent: "esphome/${name} (${project_version})"
  verify_ssl: false
  timeout: 15s

globals:
  - id: healthcheck_send_errors
    type: int
    restore_value: no
    initial_value: '-1'

web_server:
  version: 3
  sorting_groups:
    - id: healthcheck_group
      name: "❤️ HealthCheck"
      sorting_weight: -3

script:
  - id: !extend site_ping
    then:
      - logger.log: "HealthCheck..."
      - script.execute: healthcheck_ping
      - script.wait: healthcheck_ping

  - id: healthcheck_ping
    then:
      - if:
          condition:
            and:
              - wifi.connected:
              - lambda: 'return id(healthcheck_key).has_state();'
              - lambda: 'return ${lets_ping};'
          then:
            - http_request.get:
                url: !lambda |-
                  return ((std::string) "http://hc-ping.com/" + id(healthcheck_key).state).c_str();
                capture_response: false
                on_response:
                  then:
                    - lambda: id(healthcheck_set_state)->execute(response->status_code, response->duration_ms);
                on_error:
                  then:
                    - script.execute:
                        id: healthcheck_set_state
                        code: -1
                        duration: 0
            - component.update: healthcheck_errors
    mode: queued

  - id: healthcheck_set_state
    parameters:
      code: int
      duration: int
    then:
      - lambda: |-
          if (code > 0) {
            id(healthcheck_response_code).publish_state(std::to_string(code));
          } else {
            ESP_LOGE("HealthCheck", "Request failed!");
            id(healthcheck_response_code).publish_state("---");
          }

          if (duration > 0) {
            id(healthcheck_response_duration).publish_state(duration);
          } else {
            id(healthcheck_response_duration).publish_state(NAN);
          }

          if (code == 200) {
            ESP_LOGD("HealthCheck", "Send status - success");
            id(healthcheck_status).publish_state(true);
            id(healthcheck_send_errors) = 0;
          } else {
            if (code > 0) {
              ESP_LOGE("HealthCheck", "Send status - not success with response code: %d", code);
            }
            id(healthcheck_status).publish_state(false);
            id(healthcheck_send_errors) = id(healthcheck_send_errors) < 0 ? 1 : std::max(1, ++id(healthcheck_send_errors));
          }
    mode: single

text:
  - platform: template
    name: "Key"
    id: healthcheck_key
    optimistic: true
    min_length: 0
    max_length: 36
    mode: text
    restore_value: true
    icon: mdi:key-chain-variant
    entity_category: config
    disabled_by_default: true
    web_server:
      sorting_group_id: healthcheck_group
      sorting_weight: 5

binary_sensor:
  - platform: template
    name: "Status"
    id: healthcheck_status
    icon: mdi:lan-connect
    web_server:
      sorting_group_id: healthcheck_group
      sorting_weight: 1

sensor:
  - platform: template
    name: "Delivery Errors"
    id: healthcheck_errors
    lambda: |-
      if (!id(healthcheck_key).has_state() || id(healthcheck_send_errors) < 0) {
        return NAN;
      }
      return id(healthcheck_send_errors);
    accuracy_decimals: 0
    icon: mdi:space-invaders
    unit_of_measurement: "error"
    web_server:
      sorting_group_id: healthcheck_group
      sorting_weight: 3
    on_value_range:
      - above: 10.0
        then:
          - button.press: Restart

  - platform: template
    name: "Response Duration"
    id: healthcheck_response_duration
    icon: mdi:timer-outline
    entity_category: diagnostic
    disabled_by_default: true
    device_class: duration
    unit_of_measurement: "ms"
    accuracy_decimals: 0
    web_server:
      sorting_group_id: healthcheck_group
      sorting_weight: 11

text_sensor:
  - platform: template
    name: "Response Code"
    id: healthcheck_response_code
    icon: mdi:web
    entity_category: diagnostic
    disabled_by_default: true
    web_server:
      sorting_group_id: healthcheck_group
      sorting_weight: 10
