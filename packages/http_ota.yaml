button:
  - platform: template
    name: "Firmware Update"
    icon: mdi:progress-upload
    disabled_by_default: true
    entity_category: config
    on_press:
      - update.perform:
          id: new_firmware

ota:
  - platform: http_request

update:
  - platform: http_request
    id: new_firmware
    name: "Firmware Update"
    source: https://andrewjswan.github.io/svitlobot/${name}-manifest.json
    icon: mdi:update
    update_interval: 3h
    web_server:
      sorting_weight: 15

esphome:
  on_boot:
    priority: -100
    then:
      - delay: 15s
      - update.check: new_firmware
